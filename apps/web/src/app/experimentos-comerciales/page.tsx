"use client";

import { useState } from "react";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { InfoIcon } from "lucide-react";
import { toast } from "sonner";

import { Badge } from "@crm-fran/ui/components/badge";
import { Button } from "@crm-fran/ui/components/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@crm-fran/ui/components/card";
import { Empty } from "@crm-fran/ui/components/empty";
import { Field, FieldDescription, FieldGroup, FieldLabel } from "@crm-fran/ui/components/field";
import { Input } from "@crm-fran/ui/components/input";
import { Popover, PopoverContent, PopoverDescription, PopoverHeader, PopoverTitle, PopoverTrigger } from "@crm-fran/ui/components/popover";
import { Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectTrigger, SelectValue } from "@crm-fran/ui/components/select";
import { Skeleton } from "@crm-fran/ui/components/skeleton";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@crm-fran/ui/components/table";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@crm-fran/ui/components/tabs";
import { Textarea } from "@crm-fran/ui/components/textarea";

import { trpc } from "@/utils/trpc";

type DraftForm = { name: string; hypothesis: string; interventionType: "assignment_routing" | "speed_priority" | "follow_up_cadence" | "next_best_action"; primaryMetric: "contacted" | "appointment" | "show" | "sale"; profiles: string; sources: string; campaigns: string; types: string; instructions: string; allocationPercent: string; minimumSamplePerArm: string; maturationDays: string; guardrailTolerancePp: string };
const initialForm: DraftForm = { name: "", hypothesis: "", interventionType: "assignment_routing", primaryMetric: "sale", profiles: "", sources: "", campaigns: "", types: "", instructions: "", allocationPercent: "50", minimumSamplePerArm: "30", maturationDays: "14", guardrailTolerancePp: "5" };
const split = (value: string) => value.split(",").map((item) => item.trim()).filter(Boolean);
const percentage = (value: number | null | undefined) => value === null || value === undefined ? "—" : `${(value * 100).toFixed(1)}%`;

function Info({ label, title, children }: { label: string; title: string; children: string }) {
  return <Popover><PopoverTrigger render={<Button variant="ghost" size="icon-xs" aria-label={label} />}><InfoIcon aria-hidden="true" /></PopoverTrigger><PopoverContent align="start"><PopoverHeader><PopoverTitle>{title}</PopoverTitle><PopoverDescription>{children}</PopoverDescription></PopoverHeader></PopoverContent></Popover>;
}

function EmptyTab({ heading, description }: { heading: string; description: string }) { return <Card><CardContent><Empty heading={heading} description={description} /></CardContent></Card>; }

export default function CommercialExperimentsPage() {
  const client = useQueryClient(); const [form, setForm] = useState(initialForm);
  const list = useQuery(trpc.commercialExperiments.list.queryOptions());
  const invalidate = () => { void client.invalidateQueries({ queryKey: trpc.commercialExperiments.list.queryKey() }); };
  const create = useMutation(trpc.commercialExperiments.create.mutationOptions({ onSuccess: () => { toast.success("Borrador creado"); setForm(initialForm); invalidate(); }, onError: () => toast.error("No se pudo crear el borrador") }));
  if (list.isPending) return <main className="flex flex-col gap-4 p-6"><Skeleton className="h-24 w-full" /><Skeleton className="h-72 w-full" /></main>;
  if (list.isError) return <main className="p-6"><Empty heading="No se pudieron cargar los experimentos" description="Necesitas permisos de administración global para consultar esta sección." /></main>;
  const experiments = list.data ?? [];
  const createDraft = () => create.mutate({ name: form.name, hypothesis: form.hypothesis, interventionType: form.interventionType, primaryMetric: form.primaryMetric, eligibility: { profiles: split(form.profiles), sources: split(form.sources), campaigns: split(form.campaigns), types: split(form.types) as ("maestra" | "vsl")[] }, treatmentConfig: {}, treatmentInstructions: { instrucciones: form.instructions }, allocationPercent: Number(form.allocationPercent), minimumSamplePerArm: Number(form.minimumSamplePerArm), maturationDays: Number(form.maturationDays), guardrailTolerancePp: Number(form.guardrailTolerancePp) });
  return <main className="flex flex-col gap-6 p-4 sm:p-6"><header className="flex flex-col gap-2"><div className="flex items-center gap-1"><h1 className="text-2xl font-semibold">Experimentos comerciales</h1><Info label="Información sobre experimentos comerciales" title="Decisiones controladas">La aleatorización compara cohortes de control y tratamiento. Este módulo solo registra evidencia y decisiones; nunca cambia leads, alertas ni reglas de producción.</Info></div><p className="text-sm text-muted-foreground">Prueba intervenciones de forma controlada antes de proponer una regla. Toda intervención operativa sigue siendo manual.</p></header>
    <Card><CardHeader><CardTitle>Nuevo borrador</CardTitle><CardDescription>La configuración queda congelada al activar el experimento. Las instrucciones solo aparecen para la cohorte de tratamiento.</CardDescription></CardHeader><CardContent><FieldGroup><Field><FieldLabel htmlFor="experiment-name">Nombre</FieldLabel><Input id="experiment-name" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} /></Field><Field><FieldLabel htmlFor="hypothesis">Hipótesis</FieldLabel><Textarea id="hypothesis" value={form.hypothesis} onChange={(event) => setForm({ ...form, hypothesis: event.target.value })} /></Field><Field><FieldLabel>Tipo de intervención</FieldLabel><Select value={form.interventionType} onValueChange={(value) => setForm({ ...form, interventionType: value as DraftForm["interventionType"] })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectGroup><SelectLabel>Operación</SelectLabel><SelectItem value="assignment_routing">Enrutamiento de asignación</SelectItem><SelectItem value="speed_priority">Prioridad de velocidad</SelectItem><SelectItem value="follow_up_cadence">Cadencia de seguimiento</SelectItem><SelectItem value="next_best_action">Próxima mejor acción</SelectItem></SelectGroup></SelectContent></Select></Field><Field><FieldLabel>Métrica principal</FieldLabel><Select value={form.primaryMetric} onValueChange={(value) => setForm({ ...form, primaryMetric: value as DraftForm["primaryMetric"] })}><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectGroup><SelectLabel>Embudo</SelectLabel><SelectItem value="contacted">Contactado</SelectItem><SelectItem value="appointment">Agenda</SelectItem><SelectItem value="show">Show</SelectItem><SelectItem value="sale">Venta</SelectItem></SelectGroup></SelectContent></Select></Field><Field><FieldLabel htmlFor="eligibility-profile">Perfiles elegibles (opcional)</FieldLabel><Input id="eligibility-profile" placeholder="Perfil A, Perfil B" value={form.profiles} onChange={(event) => setForm({ ...form, profiles: event.target.value })} /></Field><Field><FieldLabel htmlFor="eligibility-source">Fuentes elegibles (opcional)</FieldLabel><Input id="eligibility-source" placeholder="Meta, Google" value={form.sources} onChange={(event) => setForm({ ...form, sources: event.target.value })} /></Field><Field><FieldLabel htmlFor="eligibility-campaign">Campañas elegibles (opcional)</FieldLabel><Input id="eligibility-campaign" placeholder="Campaña 1, Campaña 2" value={form.campaigns} onChange={(event) => setForm({ ...form, campaigns: event.target.value })} /></Field><Field><FieldLabel htmlFor="eligibility-type">Tipos elegibles (opcional)</FieldLabel><Input id="eligibility-type" placeholder="maestra, vsl" value={form.types} onChange={(event) => setForm({ ...form, types: event.target.value })} /><FieldDescription>Los filtros presentes se aplican conjuntamente; deja un campo vacío para no filtrarlo.</FieldDescription></Field><Field><FieldLabel htmlFor="treatment-instructions">Instrucciones para tratamiento</FieldLabel><Textarea id="treatment-instructions" value={form.instructions} onChange={(event) => setForm({ ...form, instructions: event.target.value })} /></Field><Field><FieldLabel htmlFor="allocation">Asignación a tratamiento (%)</FieldLabel><Input id="allocation" type="number" min="0" max="100" value={form.allocationPercent} onChange={(event) => setForm({ ...form, allocationPercent: event.target.value })} /></Field><Field><FieldLabel htmlFor="sample">Mínimo por brazo</FieldLabel><Input id="sample" type="number" min="1" value={form.minimumSamplePerArm} onChange={(event) => setForm({ ...form, minimumSamplePerArm: event.target.value })} /></Field><Field><FieldLabel htmlFor="maturation">Días de maduración</FieldLabel><Input id="maturation" type="number" min="0" value={form.maturationDays} onChange={(event) => setForm({ ...form, maturationDays: event.target.value })} /></Field><Field><FieldLabel htmlFor="guardrail">Tolerancia de guardrail (pp)</FieldLabel><Input id="guardrail" type="number" min="0" value={form.guardrailTolerancePp} onChange={(event) => setForm({ ...form, guardrailTolerancePp: event.target.value })} /></Field><Button onClick={createDraft} disabled={create.isPending}>Crear borrador</Button></FieldGroup></CardContent></Card>
    <Tabs defaultValue="draft"><TabsList><TabsTrigger value="draft">Borradores</TabsTrigger><TabsTrigger value="active">Activos</TabsTrigger><TabsTrigger value="results">Resultados</TabsTrigger></TabsList><TabsContent value="draft" className="flex flex-col gap-4">{experiments.filter((item) => item.status === "draft").length === 0 ? <EmptyTab heading="Sin borradores" description="Crea una hipótesis controlada para empezar." /> : experiments.filter((item) => item.status === "draft").map((item) => <ExperimentCard key={item.id} experiment={item} onChange={invalidate} />)}</TabsContent><TabsContent value="active" className="flex flex-col gap-4">{experiments.filter((item) => item.status === "active" || item.status === "stopped").length === 0 ? <EmptyTab heading="Sin experimentos activos" description="Los experimentos activados aparecerán aquí." /> : experiments.filter((item) => item.status === "active" || item.status === "stopped").map((item) => <ExperimentCard key={item.id} experiment={item} onChange={invalidate} />)}</TabsContent><TabsContent value="results" className="flex flex-col gap-4">{experiments.filter((item) => item.status === "completed").length === 0 ? <EmptyTab heading="Sin resultados cerrados" description="Completa un experimento para registrar una decisión humana." /> : experiments.filter((item) => item.status === "completed").map((item) => <ExperimentCard key={item.id} experiment={item} onChange={invalidate} />)}</TabsContent></Tabs></main>;
}

function ExperimentCard({ experiment, onChange }: { experiment: { id: string; name: string; hypothesis: string; status: "draft" | "active" | "stopped" | "completed"; interventionType: string; primaryMetric: string }; onChange: () => void }) {
  const client = useQueryClient(); const detail = useQuery(trpc.commercialExperiments.detail.queryOptions({ experimentId: experiment.id }));
  const refresh = () => { onChange(); void client.invalidateQueries({ queryKey: trpc.commercialExperiments.detail.queryKey({ experimentId: experiment.id }) }); };
  const activate = useMutation(trpc.commercialExperiments.activate.mutationOptions({ onSuccess: (value) => { toast.success(`Activado: ${value.enrollment.inserted} nuevos, ${value.enrollment.existing} existentes, ${value.enrollment.conflicts} conflictos`); refresh(); } }));
  const enroll = useMutation(trpc.commercialExperiments.enrollNew.mutationOptions({ onSuccess: (value) => { toast.success(`Cohorte actualizada: ${value.inserted} nuevos, ${value.existing} existentes, ${value.conflicts} conflictos`); refresh(); } }));
  const stop = useMutation(trpc.commercialExperiments.stop.mutationOptions({ onSuccess: refresh })); const complete = useMutation(trpc.commercialExperiments.complete.mutationOptions({ onSuccess: refresh })); const decision = useMutation(trpc.commercialExperiments.recordFinalDecision.mutationOptions({ onSuccess: refresh })); const apply = useMutation(trpc.commercialExperiments.markTreatmentApplied.mutationOptions({ onSuccess: refresh }));
  const data = detail.data;
  return <Card><CardHeader><div className="flex flex-wrap items-center gap-2"><CardTitle>{experiment.name}</CardTitle><Badge variant="outline">{experiment.status}</Badge></div><CardDescription>{experiment.hypothesis} · {experiment.interventionType} · Métrica: {experiment.primaryMetric}</CardDescription></CardHeader><CardContent className="flex flex-col gap-4">{experiment.status === "draft" ? <Button onClick={() => activate.mutate({ experimentId: experiment.id })} disabled={activate.isPending}>Activar y aleatorizar</Button> : null}{data ? <><div className="flex flex-wrap gap-2"><Badge variant="secondary">Control: {data.results.arms.control.sampleSize}</Badge><Badge variant="secondary">Tratamiento: {data.results.arms.treatment.sampleSize}</Badge><Badge variant="outline">Uplift absoluto: {data.results.primary.absolutePpUplift.toFixed(1)} pp</Badge><Badge variant="outline">Uplift relativo: {data.results.primary.relativeUplift === null ? "No calculable" : percentage(data.results.primary.relativeUplift)}</Badge><Badge variant="outline">IC 95%: {data.results.primary.confidenceInterval95.lowerPp?.toFixed(1) ?? "—"} a {data.results.primary.confidenceInterval95.upperPp?.toFixed(1) ?? "—"} pp</Badge><Badge variant={data.results.guardrail.isHarm ? "destructive" : "outline"}>Guardrail: {data.results.guardrail.isHarm ? "posible daño" : "sin alerta"}</Badge><Badge variant="outline">Cumplimiento: {percentage(data.results.compliance.rate)}</Badge><Badge variant="outline">Evidencia: {data.results.state}</Badge></div>{experiment.status === "active" || experiment.status === "stopped" ? <div className="flex flex-wrap gap-2">{experiment.status === "active" ? <><Button variant="outline" onClick={() => enroll.mutate({ experimentId: experiment.id })}>Incorporar nuevos</Button><Button variant="outline" onClick={() => stop.mutate({ experimentId: experiment.id })}>Detener</Button></> : null}<Button onClick={() => complete.mutate({ experimentId: experiment.id })}>Completar</Button></div> : null}{experiment.status === "completed" ? <div className="flex flex-col gap-2"><p className="text-sm text-muted-foreground">Aprobar solo registra una decisión; nunca cambia reglas de producción.</p><div className="flex flex-wrap gap-2"><Button onClick={() => decision.mutate({ experimentId: experiment.id, decision: "approved", notes: "Aprobada tras revisión humana" })}>Aprobar regla</Button><Button variant="outline" onClick={() => decision.mutate({ experimentId: experiment.id, decision: "rejected", notes: "Rechazada tras revisión humana" })}>Rechazar</Button><Button variant="outline" onClick={() => decision.mutate({ experimentId: experiment.id, decision: "inconclusive", notes: "Evidencia inconclusa" })}>Inconcluso</Button></div>{data.finalDecision ? <p className="text-sm text-muted-foreground">Decisión: {data.finalDecision} · {data.finalDecisionNotes ?? "Sin notas"}</p> : null}</div> : null}<div className="overflow-x-auto"><Table><TableHeader><TableRow><TableHead>Lead</TableHead><TableHead>Contexto congelado</TableHead><TableHead>Brazo</TableHead><TableHead>Maduración</TableHead><TableHead>Tratamiento</TableHead></TableRow></TableHeader><TableBody>{data.assignments.length === 0 ? <TableRow><TableCell colSpan={5}><Empty heading="Sin cohortes" description="La aleatorización registrará cohortes al activar." /></TableCell></TableRow> : data.assignments.map((assignment) => <TableRow key={assignment.id}><TableCell>{assignment.leadId}</TableCell><TableCell>{String(assignment.frozenContext.profile ?? "Sin perfil")} · {String(assignment.frozenContext.source ?? "Sin fuente")}</TableCell><TableCell><Badge variant="outline">{assignment.arm}</Badge></TableCell><TableCell>{assignment.isMature ? "Maduro" : "Pendiente"}</TableCell><TableCell>{assignment.arm === "treatment" ? <div className="flex flex-col gap-2"><span>{String(assignment.treatmentInstructions?.instrucciones ?? "Sin instrucciones")}</span>{experiment.status === "active" && !assignment.treatmentAppliedAt ? <Button variant="outline" size="sm" onClick={() => apply.mutate({ assignmentId: assignment.id })}>Marcar aplicada</Button> : assignment.treatmentAppliedAt ? <Badge variant="secondary">Aplicada</Badge> : <span className="text-muted-foreground">Tratamiento cerrado</span>}</div> : <span className="text-muted-foreground">Control: sin instrucciones</span>}</TableCell></TableRow>)}</TableBody></Table></div></> : <Skeleton className="h-32 w-full" />}</CardContent></Card>;
}